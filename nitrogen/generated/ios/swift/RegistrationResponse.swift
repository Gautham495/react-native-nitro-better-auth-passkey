///
/// RegistrationResponse.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `RegistrationResponse`, backed by a C++ struct.
 */
public typealias RegistrationResponse = margelo.nitro.nitrobetterauthpasskey.RegistrationResponse

public extension RegistrationResponse {
  private typealias bridge = margelo.nitro.nitrobetterauthpasskey.bridge.swift

  /**
   * Create a new instance of `RegistrationResponse`.
   */
  init(id: String, rawId: String, type: String, clientDataJSON: String, attestationObject: String, transports: [String], authenticatorAttachment: String?) {
    self.init(std.string(id), std.string(rawId), std.string(type), std.string(clientDataJSON), std.string(attestationObject), { () -> bridge.std__vector_std__string_ in
      var __vector = bridge.create_std__vector_std__string_(transports.count)
      for __item in transports {
        __vector.push_back(std.string(__item))
      }
      return __vector
    }(), { () -> bridge.std__optional_std__string_ in
      if let __unwrappedValue = authenticatorAttachment {
        return bridge.create_std__optional_std__string_(std.string(__unwrappedValue))
      } else {
        return .init()
      }
    }())
  }

  @inline(__always)
  var id: String {
    return String(self.__id)
  }
  
  @inline(__always)
  var rawId: String {
    return String(self.__rawId)
  }
  
  @inline(__always)
  var type: String {
    return String(self.__type)
  }
  
  @inline(__always)
  var clientDataJSON: String {
    return String(self.__clientDataJSON)
  }
  
  @inline(__always)
  var attestationObject: String {
    return String(self.__attestationObject)
  }
  
  @inline(__always)
  var transports: [String] {
    return self.__transports.map({ __item in String(__item) })
  }
  
  @inline(__always)
  var authenticatorAttachment: String? {
    return { () -> String? in
      if bridge.has_value_std__optional_std__string_(self.__authenticatorAttachment) {
        let __unwrapped = bridge.get_std__optional_std__string_(self.__authenticatorAttachment)
        return String(__unwrapped)
      } else {
        return nil
      }
    }()
  }
}
